import { Resetpassbody } from "../../components/Resetpass/Resetpass";
import { Title } from "../../components/Title/Title";

export const Resetpass = () => {
    Title (" | Reset Password");
    
        return(
            <>
              < Resetpassbody />  
            </>
        );
}