import { Link } from "react-router-dom";
import { Title } from "../../components/Title/Title";

export const Contact = () => {
    Title (" | Contact");

    return (
        <div>
            <h2>Contact</h2>
        </div>
    );
}