import { Link } from "react-router-dom";
import { Title } from "../../components/Title/Title";

export const About = () => {
    Title (" | About");

    return (
        <div>
            <h2>About</h2>
        </div>
    );
}